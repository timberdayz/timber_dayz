from backend.domains.platform.routers.auth import *  # noqa: F403

# Compatibility shim: preserve legacy import path (`backend.routers.auth`) while
# the router is physically relocated under the platform domain.
