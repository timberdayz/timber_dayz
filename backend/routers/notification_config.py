from backend.domains.platform.routers.notification_config import *  # noqa: F403

# Compatibility shim: preserve legacy import path (`backend.routers.notification_config`)
# for tests and any external imports while the router is physically relocated.
