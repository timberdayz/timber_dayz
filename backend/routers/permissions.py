from backend.domains.platform.routers.permissions import *  # noqa: F403

# Compatibility shim: preserve legacy import path (`backend.routers.permissions`) while
# the router is physically relocated under the platform domain.

