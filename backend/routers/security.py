from backend.domains.platform.routers.security import *  # noqa: F403

# Compatibility shim: preserve legacy import path (`backend.routers.security`) while
# the router is physically relocated under the platform domain.
