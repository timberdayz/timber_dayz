# ============================================================================
# 西虹ERP系统 - 生产环境配置示例
# ============================================================================
# 复制此文件为 .env 并放在项目根目录
# 生产环境必须修改所有密钥和密码！

# 数据库配置
POSTGRES_HOST=your_production_host
POSTGRES_PORT=5432
POSTGRES_USER=erp_user
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
POSTGRES_DB=xihong_erp

# Superset配置
SUPERSET_URL=https://your-superset-domain.com
SUPERSET_USERNAME=admin
SUPERSET_PASSWORD=CHANGE_THIS_PASSWORD
SUPERSET_GUEST_TOKEN_SECRET=CHANGE_THIS_TO_STRONG_SECRET_KEY

# 安全配置
API_SECRET_KEY=CHANGE_THIS_SECRET_KEY
JWT_SECRET_KEY=CHANGE_THIS_JWT_SECRET

# Redis配置
# ⭐ v4.19.5 更新：支持两种配置方式
# 方式1：直接使用 REDIS_URL（推荐，支持密码）
REDIS_URL=redis://:CHANGE_THIS_PASSWORD@your_redis_host:6379/0

# 方式2：使用独立配置项（如果未设置 REDIS_URL，将自动构建）
# REDIS_HOST=your_redis_host
# REDIS_PORT=6379
# REDIS_PASSWORD=CHANGE_THIS_PASSWORD
# REDIS_DB=0

# Celery配置（使用相同的 Redis 密码）
CELERY_BROKER_URL=redis://:CHANGE_THIS_PASSWORD@your_redis_host:6379/0
CELERY_RESULT_BACKEND=redis://:CHANGE_THIS_PASSWORD@your_redis_host:6379/0

# 日志级别
LOG_LEVEL=WARNING

# 功能开关
DEBUG=false
ENABLE_SUPERSET=true
ENABLE_REDIS=true
ENABLE_MV_AUTO_REFRESH=true

