# 数据质量保障指南

**版本**: v1.0.0  
**创建日期**: 2025-01-31  
**用途**: C类数据核心字段优化计划（Phase 2）

---

## 一、概述

本文档提供数据质量保障的完整指南，包括B类数据完整性检查、数据隔离区管理、数据质量监控等内容。

### 1.1 数据质量保障体系

**三层保障机制**:
1. **数据入库前验证**: 格式验证、必填字段验证（现有验证函数）
2. **数据入库后检查**: C类计算所需字段完整性检查（新增）
3. **数据计算前检查**: C类数据计算前的最终验证（新增）

---

## 二、B类数据完整性检查

### 2.1 检查服务

**服务位置**: `backend/services/c_class_data_validator.py`

**核心功能**:
- 检查B类数据是否包含C类计算所需的核心字段
- 验证字段数据质量（非空、数据类型正确）
- 生成数据质量报告
- 自动标记数据质量问题

**职责边界**:
- ✅ 关注C类计算所需字段的完整性（字段是否存在、是否有值）
- ❌ 不关注数据格式验证（由现有验证函数处理）
- ✅ 在入库后或计算前执行
- ❌ 不阻止数据入库（只记录警告）

---

### 2.2 检查方法

#### 2.2.1 单次检查

**方法**: `check_b_class_completeness()`

**参数**:
- `platform_code`: 平台代码
- `shop_id`: 店铺ID
- `metric_date`: 指标日期
- `data_domain`: 数据域（可选）

**返回**:
```python
{
    "orders_complete": False,  # 订单数据完整性
    "products_complete": False,  # 产品数据完整性
    "inventory_complete": False,  # 库存数据完整性
    "core_fields_present": [],  # 核心字段存在性
    "missing_fields": [],  # 缺失字段列表
    "data_quality_score": 0.0,  # 数据质量评分（0-100）
    "warnings": []  # 警告信息列表
}
```

**使用示例**:
```python
from backend.services.c_class_data_validator import get_c_class_data_validator

validator = get_c_class_data_validator(db)
result = validator.check_b_class_completeness(
    platform_code="shopee",
    shop_id="shop001",
    metric_date=date(2025, 1, 31)
)

if not result["orders_complete"]:
    logger.warning(f"订单数据不完整: {result['missing_fields']}")
```

---

#### 2.2.2 批量检查（生成报告）

**方法**: `generate_quality_report()`

**参数**:
- `platform_code`: 平台代码
- `shop_id`: 店铺ID
- `start_date`: 开始日期
- `end_date`: 结束日期

**返回**:
```python
{
    "platform_code": "shopee",
    "shop_id": "shop001",
    "date_range": {"start": "2024-01-01", "end": "2024-01-31"},
    "daily_checks": [
        {
            "date": "2024-01-01",
            "orders_complete": True,
            "products_complete": False,
            "data_quality_score": 85.0,
            "missing_fields": ["products.conversion_rate"]
        },
        ...
    ],
    "summary": {
        "total_days": 31,
        "avg_quality_score": 90.5,
        "complete_days": 25,
        "incomplete_days": 6
    }
}
```

---

### 2.3 集成点

#### 2.3.1 数据入库后检查

**位置**: `backend/routers/field_mapping.py::ingest_file()`

**集成方式**:
```python
# 数据入库成功后，异步调用完整性检查
if ingest_result["success"]:
    # 异步检查B类数据完整性（不阻塞入库流程）
    asyncio.create_task(
        check_b_class_completeness_async(
            platform_code=platform_code,
            shop_id=shop_id,
            metric_date=date.today()
        )
    )
```

**注意事项**:
- ✅ 检查逻辑异步执行，不阻塞数据入库流程
- ✅ 检查结果仅记录警告，不阻止入库
- ✅ 检查失败不影响入库成功状态

---

#### 2.3.2 C类数据计算前检查

**位置**: `backend/services/shop_health_service.py::calculate_health_score()`

**集成方式**:
```python
# C类数据计算前，检查B类数据完整性
validator = get_c_class_data_validator(db)
check_result = validator.check_b_class_completeness(
    platform_code=platform_code,
    shop_id=shop_id,
    metric_date=metric_date
)

if not check_result["orders_complete"] or not check_result["products_complete"]:
    logger.warning(f"B类数据不完整，可能影响C类数据计算准确性: {check_result['missing_fields']}")
    # 继续计算，但记录警告
```

**注意事项**:
- ✅ 检查失败不阻止计算，只记录警告
- ✅ 确保C类数据计算能够正常进行（即使数据不完整）

---

## 三、数据隔离区管理

### 3.1 错误类型扩展

**新增错误类型**:
- `missing_c_class_core_field` - C类数据计算所需核心字段缺失
- `invalid_currency_policy` - 货币策略不符合要求
- `data_domain_mismatch` - 数据域不匹配

**错误类型定义**: `backend/routers/data_quarantine.py`

```python
ERROR_TYPES = {
    "missing_c_class_core_field": "C类数据计算所需核心字段缺失",
    "invalid_currency_policy": "货币策略不符合要求",
    "data_domain_mismatch": "数据域不匹配",
    "validation_error": "数据验证失败",
    "data_type_error": "数据类型错误",
    "required_field_missing": "必填字段缺失",
    "unknown": "未知错误"
}
```

---

### 3.2 字段缺失详情显示

**API端点**: `GET /api/data-quarantine/detail/{quarantine_id}`

**响应字段**:
```json
{
    "success": true,
    "data": {
        "id": 1,
        "error_type": "missing_c_class_core_field",
        "error_type_label": "C类数据计算所需核心字段缺失",
        "missing_fields": ["orders.total_amount_rmb", "products.conversion_rate"],
        "raw_data": {...},
        ...
    }
}
```

---

### 3.3 重新处理流程

**API端点**: `POST /api/data-quarantine/reprocess`

**流程**:
1. 读取隔离数据的`raw_data`
2. 应用`corrections`（如果有）
3. 如果是C类数据字段缺失错误，检查字段是否已补充
4. 重新验证数据
5. 重新入库或继续隔离

**C类数据字段缺失错误处理**:
```python
if quarantine.error_type == "missing_c_class_core_field":
    # 使用C类数据验证器检查完整性
    validator = get_c_class_data_validator(db)
    check_result = validator.check_b_class_completeness(...)
    
    if not check_result.get("orders_complete", True):
        # 仍有字段缺失，提示用户补充
        return {"error": "C类数据计算所需字段仍缺失，请先补充缺失字段后再重新处理"}
```

---

## 四、数据质量监控API

### 4.1 C类数据计算就绪状态查询

**API端点**: `GET /api/data-quality/c-class-readiness`

**参数**:
- `platform_code`: 平台代码（可选）
- `shop_id`: 店铺ID（可选）
- `metric_date`: 指标日期（可选，默认今天）

**响应**:
```json
{
    "success": true,
    "data": {
        "c_class_ready": true,
        "b_class_completeness": {
            "orders": 100.0,
            "products": 95.0,
            "inventory": 100.0
        },
        "missing_core_fields": [],
        "data_quality_score": 98.5,
        "warnings": [],
        "timestamp": "2025-01-31T10:00:00"
    }
}
```

---

### 4.2 B类数据完整性检查

**API端点**: `GET /api/data-quality/b-class-completeness`

**参数**:
- `platform_code`: 平台代码（必填）
- `shop_id`: 店铺ID（必填）
- `start_date`: 开始日期（可选，默认30天前）
- `end_date`: 结束日期（可选，默认今天）

**响应**:
```json
{
    "success": true,
    "data": {
        "platform_code": "shopee",
        "shop_id": "shop001",
        "date_range": {"start": "2024-01-01", "end": "2024-01-31"},
        "daily_checks": [...],
        "summary": {
            "total_days": 31,
            "avg_quality_score": 90.5,
            "complete_days": 25,
            "incomplete_days": 6
        }
    }
}
```

---

### 4.3 核心字段状态查询

**API端点**: `GET /api/data-quality/core-fields-status`

**参数**:
- `data_domain`: 数据域（可选）

**响应**:
```json
{
    "success": true,
    "data": {
        "total_fields": 17,
        "present_fields": 15,
        "missing_fields": 2,
        "fields_by_domain": {
            "orders": {
                "total": 6,
                "present": 6,
                "missing": 0,
                "present_fields": [...],
                "missing_fields": []
            },
            ...
        },
        "timestamp": "2025-01-31T10:00:00"
    }
}
```

---

## 五、数据质量评分标准

### 5.1 评分计算方法

**公式**:
```
数据质量评分 = (已存在字段数 / 总字段数) * 100

总字段数 = 17个核心字段
已存在字段数 = 在field_mapping_dictionary表中存在且值不为NULL的字段数
```

---

### 5.2 质量等级

| 等级 | 评分范围 | 说明 | 建议操作 |
|------|---------|------|---------|
| **优秀** | 90-100分 | 所有核心字段完整，C类数据计算就绪 | 无需操作 |
| **良好** | 70-89分 | 大部分核心字段完整，部分C类数据可计算 | 补充缺失字段 |
| **一般** | 50-69分 | 核心字段缺失较多，C类数据计算受限 | 优先补充关键字段 |
| **较差** | <50分 | 核心字段严重缺失，C类数据无法计算 | 立即补充所有缺失字段 |

---

## 六、数据质量改进流程

### 6.1 识别数据质量问题

**步骤**:
1. 运行`scripts/verify_c_class_core_fields.py`验证字段存在性
2. 调用`GET /api/data-quality/c-class-readiness`检查就绪状态
3. 调用`GET /api/data-quality/b-class-completeness`检查数据完整性
4. 查看数据隔离区，识别字段缺失错误

---

### 6.2 补充缺失字段

**步骤**:
1. 运行`scripts/add_c_class_missing_fields.py`补充缺失字段到字段映射辞典
2. 配置字段映射模板，确保字段映射完整性
3. 重新验证字段存在性

---

### 6.3 修复数据质量问题

**步骤**:
1. 查看数据隔离区，识别数据质量问题
2. 补充缺失字段或修正数据错误
3. 重新处理隔离数据
4. 验证数据质量改进效果

---

## 七、最佳实践

### 7.1 数据入库前

**建议**:
- ✅ 使用字段映射模板确保字段映射完整性
- ✅ 验证Excel文件包含所有必需字段
- ✅ 检查货币策略合规性

---

### 7.2 数据入库后

**建议**:
- ✅ 自动运行B类数据完整性检查
- ✅ 记录数据质量评分
- ✅ 标记数据质量问题到隔离区

---

### 7.3 C类数据计算前

**建议**:
- ✅ 检查B类数据完整性
- ✅ 验证核心字段存在性
- ✅ 记录数据质量警告（不阻止计算）

---

## 八、监控和告警

### 8.1 数据质量监控指标

**关键指标**:
- C类数据计算就绪率（%）
- B类数据完整性评分（0-100）
- 核心字段缺失数量
- 数据隔离区记录数量

---

### 8.2 告警阈值

**告警规则**:
- ⚠️ **警告**: 数据质量评分 < 90分
- 🔴 **严重**: 数据质量评分 < 70分
- 🚨 **紧急**: 核心字段缺失数量 > 5个

---

## 九、常见问题

### 9.1 Q: 数据质量检查会影响数据入库性能吗？

**A**: 
- 检查逻辑异步执行，不阻塞数据入库流程
- 或入库后异步检查，结果记录到日志和数据质量表
- 批量检查时使用数据库聚合查询，避免逐条检查

---

### 9.2 Q: 如果数据质量检查失败，会阻止C类数据计算吗？

**A**: 
- 不会。检查失败只记录警告，不阻止计算
- 确保C类数据计算能够正常进行（即使数据不完整）
- 计算结果会标注数据质量警告

---

### 9.3 Q: 如何提高数据质量评分？

**A**: 
1. 运行`scripts/add_c_class_missing_fields.py`补充缺失字段
2. 配置字段映射模板，确保字段映射完整性
3. 修复数据隔离区中的数据质量问题
4. 重新处理隔离数据

---

## 十、更新记录

| 版本 | 日期 | 更新内容 |
|------|------|---------|
| v1.0.0 | 2025-01-31 | 初始版本，定义数据质量保障体系 |

---

## 十一、相关文档

- [C类数据核心字段定义](C_CLASS_DATA_CORE_FIELDS.md)
- [货币策略文档](CURRENCY_POLICY.md)
- [字段映射用户指南](USER_GUIDE_FIELD_MAPPING.md)

