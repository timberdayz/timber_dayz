# 西虹ERP系统 - 快速命令参考卡

## 🚀 重启后启动（3条命令）

```bash
# 1. 启动PostgreSQL（Docker）
docker-compose up -d postgres

# 2. 快速诊断
python diagnose_simple.py

# 3. 启动系统
python run_fixed.py
```

---

## 🎯 常用命令

### 启动系统

```bash
# 完整启动（推荐）
python run_fixed.py

# 只启动后端
python run_fixed.py --backend-only

# 只启动前端
python run_fixed.py --frontend-only

# 手动启动后端
cd backend
python -m uvicorn main:app --host 0.0.0.0 --port 8001 --reload

# 手动启动前端
cd frontend
npm run dev
```

### 诊断系统

```bash
# 全面诊断
python diagnose_simple.py

# 测试后端
curl http://localhost:8001/health

# 测试前端
curl http://localhost:5173
```

### Docker管理

```bash
# 启动PostgreSQL
docker-compose up -d postgres

# 查看容器状态
docker ps

# 查看日志
docker logs xihong_erp_postgres

# 停止容器
docker-compose down
```

---

## 🌐 访问地址

| 服务 | 地址 | 用途 |
|------|------|------|
| 前端界面 | http://localhost:5173 | 主系统入口 |
| API文档 | http://localhost:8001/api/docs | 查看所有API |
| 健康检查 | http://localhost:8001/health | 系统状态 |
| pgAdmin | http://localhost:5051 | 数据库管理 |
| 字段映射 | http://localhost:5173/#/field-mapping | 字段映射功能 |

---

## ⚡ 快速验证

```bash
# 验证后端（应该返回 healthy）
curl http://localhost:8001/health

# 验证路由数量（应该返回 110）
curl http://localhost:8001/health | grep "total"

# 验证数据库（应该返回 connected）
curl http://localhost:8001/health | grep "database"
```

---

## 🐛 故障排除

| 问题 | 命令 |
|------|------|
| PostgreSQL未运行 | `docker-compose up -d postgres` |
| 端口被占用 | `netstat -ano \| findstr ":8001"` |
| 检查Python进程 | `tasklist \| findstr python` |
| 强制停止端口 | `taskkill /F /PID <PID>` |

---

## 📊 性能指标

| 指标 | 目标 | 实际 |
|------|------|------|
| 启动时间 | <15秒 | **0.34秒** ✅ |
| 注册路由 | 全部 | **110个** ✅ |
| 并发支持 | 50人 | **100连接** ✅ |
| 响应时间 | <3秒 | **<1秒** ✅ |

---

## 💾 保存此卡片

建议打印或保存此文档，重启电脑后快速查看。

**文件位置**: `F:\Vscode\python_programme\AI_code\xihong_erp\快速命令参考.md`

---

**版本**: v4.1.1  
**更新**: 2025-10-25

