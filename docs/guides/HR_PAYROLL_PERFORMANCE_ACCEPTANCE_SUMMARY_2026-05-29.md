# HR / 绩效 / 工资链路收口验收结论（2026-05-29）

## 1. 本轮目标

本轮工作的目标是把以下主链路收口到“真实可用、可解释、可审计”的状态：

```text
店铺归属与提成比例
-> 店铺绩效
-> 个人绩效
-> 提成结果
-> 工资单
-> 我的收入
-> 月结快照
```

本轮明确排除项：

- 重点商品维度 `key_product`

## 2. 已完成项

### 2.1 薪资与绩效口径重构

- 固定薪资与绩效工资边界已拆清
- 绩效工资改为：

```text
绩效工资 = 绩效包 × 个人绩效系数
```

- 提成口径保持为：

```text
提成 = 店铺利润基数 × 可分配利润率 × 个人提成比例 × 店铺绩效系数
```

### 2.2 个人绩效独立建模

- 已启用 `employee_performance_inputs`
- 已完成个人绩效输入项 CRUD
- 已完成默认模板预览与一键套用

### 2.3 运营目标边界收口

- `SalesTarget(target_type="operation")` 已引入 `scope_type`
- 当前只允许 `scope_type=shop`
- 个人运营相关内容已收口到 `employee_performance_inputs`

### 2.4 月结快照与我的收入

- approved 月份优先读 snapshot
- draft 月份继续读 runtime
- “我的收入”坚持工资单真源，不再回退拼装中间表
- 已补解释层与四张摘要卡片

### 2.5 店铺绩效关键维度

当前正式可用维度：

- `sales`
- `profit`
- `operation`

当前不纳入正式口径：

- `key_product`

本轮对 `profit` 维度完成了真实可用修复：

- 修复店铺绩效事务边界问题，避免 `PerformanceScore` 在提交前被后续 rollback 冲掉
- 当 `target_breakdown.target_profit_amount=0` 时，按父级店铺目标利润按销售目标占比分摊兜底

### 2.6 版本提示与审计能力

- “我的收入”已补 stale 提示
- HR 工资单页已补 stale / 锁定提示
- 已新增“员工月度收入审计”视图

### 2.7 运行稳定性修复

- `employee_tasks` 的 `performance-confirmation:{period}:{employee}` 已修成幂等复用
- Redis / 缓存删除失败日志已降噪，不再污染主流程

## 3. 关键验证结果

本轮已通过的核心验证包括但不限于：

- 个人绩效输入相关后端测试
- 月结快照 schema / service / my income 专用测试
- 工资单路由与 stale 提示测试
- MyIncome 视图模型测试
- 店铺绩效 `profit` 维度回归测试
- 前端路由拆分与页面契约测试

真实环境验证结论：

- 李四（`EMP260004`）的 `2026-03` 链路已成功做过完整演练
- 在补齐个人绩效输入、绩效包并回退工资单后，链路可稳定产生：
  - 个人绩效结果
  - 提成结果
  - 工资单更新
  - 我的收入同步展示

## 4. 上线结论

如继续排除“重点商品维度”这一项，本轮范围内系统已经达到可上线状态。

理由：

- 主业务链路已闭环
- 关键页面已具备解释能力
- 关键版本冲突已可见
- 审计视图已具备排障能力
- 主要运行噪音已收敛

## 5. 当前不阻塞上线但仍需治理的事项

### 5.1 数据治理

- `target_breakdown` 历史重复数据进一步清理
- 已实现“同月只读取最新 active 店铺目标版本”的运行规则

### 5.2 结构治理

- HR / 绩效相关 schema 漂移逐步迁移补齐
- 历史乱码文件持续清理

### 5.3 业务扩展

- 重点商品维度设计与落地

## 6. 已确认的 schema 漂移

详见：

- `docs/guides/HR_PERFORMANCE_SCHEMA_DRIFT_AUDIT_2026-05-29.md`

当前已知差异已通过 migration 补齐，但仍应继续保持结构治理意识：

- `a_class.salary_structures.remark`
- `c_class.performance_scores.calculated_at`
- `core.dim_shops.is_active`

## 7. 建议的后续路线

上线后建议按以下顺序继续治理：

1. `target_breakdown` 历史重复数据进一步清理
2. schema 漂移迁移补齐
3. 历史乱码文件清理
4. 重点商品维度设计

## 8. 最终结论

本轮范围内：

- 业务闭环：已完成
- 审计能力：已完成
- 版本提示：已完成
- 模板化录入：已完成
- 运行稳定性：已达可上线水平

结论：

**在排除重点商品维度的前提下，可以作为可验收、可上线版本收口。**
