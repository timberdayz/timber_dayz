# Tasks: 业务概览全局日期同步

## 1. 新增全局日期组件

- [x] 1.1 在 `BusinessOverview.vue` 页面头部（副标题下方）新增全局日期区域
- [x] 1.2 实现粒度选择（日/周/月）和日期选择器，复用现有 value-format 逻辑避免 UTC 偏差
- [x] 1.3 默认值：粒度=月，日期=当前月份（YYYY-MM）

## 2. 状态与同步逻辑

- [x] 2.1 定义 `globalDate`（粒度+日期）、`useGlobalDate: Record<string, boolean>` 状态
- [x] 2.2 实现 `onGlobalDateChange` / `onGlobalGranularityChange` 触发同步，仅更新 `useGlobalDate[module]=true` 的模块
- [x] 2.3 实现经营指标日期映射：全局周→该周周一，全局月→该月1日

## 3. 各模块改造

- [x] 3.1 数据对比：默认 useGlobalDate=true，日期变更时检查是否手动修改
- [x] 3.2 店铺赛马：同上
- [x] 3.3 流量排名：同上
- [x] 3.4 经营指标：同上，应用周→周一、月→1日映射
- [x] 3.5 核心 KPI：可选跟随（全局为月时同步）— 第二版已实现
- [x] 3.6 清仓排名：可选跟随（月/周分别对齐）— 已实现，周度使用 type=week 周度日历

## 4. 手动覆盖与恢复

- [x] 4.1 各模块日期旁添加「跟随全局」开关（图标或按钮）
- [x] 4.2 用户修改模块日期时，设置 useGlobalDate[module]=false
- [x] 4.3 点击「跟随全局」时，恢复 useGlobalDate[module]=true 并同步全局日期

## 5. 测试与验证

- [x] 5.1 验证全局日期变更时，跟随的模块正确更新
- [x] 5.2 验证手动修改某模块后，该模块不再受全局影响
- [x] 5.3 验证恢复「跟随全局」后，模块日期与全局一致
- [x] 5.4 验证经营指标周/月映射正确（周一、1日）
